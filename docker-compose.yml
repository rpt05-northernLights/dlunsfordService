version: '2'

services:
  myapp:
    container_name: my_app
    build: .
    ports:
      - "8080:8080"
    links:
      - mongo
  
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - /data/mongodb/db:/data/db
    ports:
      - "27017:27017"